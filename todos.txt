- IF HISTORIZING IS TRUE, SET ACCESSLEVEL TO MINIMUM 5. Although maybe not always?
    - Access levels:
        1 (0x01)	CurrentRead	Read the Current Value
        2 (0x02)	CurrentWrite	Write the Current Value
        4 (0x04)	HistoryRead	Read the Historical Value(s)
        8 (0x08)	HistoryWrite	Insert Historical Value(s)

- Node currently requires a lot of stuff in its initializer (full nid + bn + class + ns).
    Should fix that.

- Namespace array is accessed in multiple places.

- Make loader return a namespacecontext

- Make Reference a pure data object. Resolve and find through indices in Namespace.